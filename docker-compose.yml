services:
  rag-api:
    build: .
    container_name: cv_rag_api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ../CVs:/app/CVs
      - vector_db:/app/vector_db
    restart: unless-stopped

volumes:
  vector_db: